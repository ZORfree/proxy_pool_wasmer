[project]
name = "hello-wasmer"
version = "0.1.0"
description = "A premium Hello World demo for Wasmer Edge"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "jinja2",
]

[tool.uv]
managed = true

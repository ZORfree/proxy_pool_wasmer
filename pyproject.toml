[project]
name = "proxy-pool"
version = "0.1.0"
description = "HTTP/SOCKS Proxy Pool for Wasmer Edge"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "jinja2",
    "pymysql",
    "httpx",
]

[tool.uv]
managed = true
